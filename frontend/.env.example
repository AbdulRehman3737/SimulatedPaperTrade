# Base URL for the backend API. Leave unset in dev to use the Vite proxy (/api -> http://localhost:4000).
# In production, point this at your deployed backend, e.g. https://api.example.com/api
VITE_API_URL=/api
